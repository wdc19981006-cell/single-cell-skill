interface:
  display_name: "GEO Single-cell Loader"
  short_description: "Inspect GEO, confirm groups, build validated Seurat"
  default_prompt: "Use $geo-single-cell-loader to inspect GSE231993 and report sample sources and disease backgrounds before asking me to define groups."
policy:
  allow_implicit_invocation: true
